# Commerce HTTP client authentication configuration
# In case the app needs to communicate with Adobe Commerce, the following
# environment variables are required depending on the authentication method used.
# Inject these variables into your actions using input parameters.

# ------------------------------------------------------------------------------
# Option 1: Authenticate with IMS (SaaS and PaaS with IMS enabled)
# Will be populated automatically when the app is built for the first time
# or can be synced on demand using `npm run sync-oauth-credentials`.
# To configure the Commerce instance to allow these credentials please follow:
# https://developer.adobe.com/commerce/extensibility/starter-kit/checkout/connect/#adobe-identity-management-service-ims
# Note: IMS secrets can be provided either as a single value (OAUTH_CLIENT_SECRET)
# or as a JSON array (OAUTH_CLIENT_SECRETS). Scopes should be a JSON array as well.
# If you only have an IMS OAuth client that issues client-credentials tokens,
# fill in OAUTH_CLIENT_ID, OAUTH_CLIENT_SECRET(S), and OAUTH_SCOPES; the
# technical account, email, and org ID fields can be left blank in that case.
# ------------------------------------------------------------------------------

OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_CLIENT_SECRETS=[""]
OAUTH_TECHNICAL_ACCOUNT_ID=
OAUTH_TECHNICAL_ACCOUNT_EMAIL=
OAUTH_SCOPES=[""]
OAUTH_IMS_ORG_ID=
AIO_CLI_ENV=prod

# ------------------------------------------------------------------------------
# Option 2: Authenticate with Commerce integration (PaaS, OAuth1.0a)
# For Adobe Commerce PaaS, you can authenticate using a Commerce Integration:
# Admin > System > Integrations > Add New
# Copy:
#   - consumer_key
#   - consumer_secret
#   - access_token
#   - access_token_secret
# And map them to:
# ------------------------------------------------------------------------------

COMMERCE_CONSUMER_KEY=
COMMERCE_CONSUMER_SECRET=
COMMERCE_ACCESS_TOKEN=
COMMERCE_ACCESS_TOKEN_SECRET=

# ------------------------------------------------------------------------------
# General Commerce configuration (SaaS and PaaS)
# Commerce REST endpoint should finish with slash '/'.
# Examples:
#  - Commerce Admin REST endpoints:
#    https://{commerce_instance_url}/rest/{store_view_code}/
#  - Adobe Commerce Cloud as a Service:
#    https://na1.api.commerce.adobe.com/{tenant_code}/
# ------------------------------------------------------------------------------

COMMERCE_BASE_URL=

# Required if webhooks are used and signature verification is enabled
COMMERCE_WEBHOOKS_PUBLIC_KEY=
